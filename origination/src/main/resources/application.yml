spring:
  datasource:
    url:
      jdbc:postgresql://db:5432/origination
    username:
      postgres
    driver-class-name:
      org.postgresql.Driver
    password:
      123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log:
      classpath:db/changelog/db.changelog-master.xml
  sql:
    init:
      mode: always

server:
  port: 8084

grpc:
  server:
    port: 9094
    enable-reflection: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    db:
      enabled: false