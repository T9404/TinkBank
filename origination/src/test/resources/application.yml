spring:
  liquibase:
    change-log:
      classpath:db/changelog/db.changelog-master.xml
  datasource:
    url:
      jdbc:postgresql://localhost:5432/origination
    username:
      postgres
    driver-class-name:
      org.postgresql.Driver
    password:
      123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always

grpc:
  client:
    stocks:
      address: in-process:test
      enableKeepAlive:
      negotiationType:
  server:
    inProcessName: test
    port: -1